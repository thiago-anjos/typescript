import lodash from './module';

const arrayNumber = [100, 200, 300];

console.log(lodash.mul(arrayNumber));

const person = lodash.bla();
const person2 = new person('thiago');
console.log(person2);

console.log(global.MYGLOBAL);
